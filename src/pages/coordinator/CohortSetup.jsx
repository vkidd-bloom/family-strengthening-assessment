// Create a new cohort — collects agency name and cohort label,
// generates a unique shareable respondent link
export default function CohortSetup() {
  return <div>Cohort Setup — placeholder</div>;
}
